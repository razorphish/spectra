# Loaded automatically by Nx from the workspace root (see Nx dotenv loading).
# Angular 21 @angular/build loads ESM-only @angular/compiler-cli via require(); Node needs this flag (20.18+).
NODE_OPTIONS=--experimental-require-module
