# Per-service local dev overrides — copy to `.env.development` (gitignored).
# Nx loads `.env.development` automatically when `nx serve aviate-api` runs the
# default `development` configuration.
PORT=3001
HOST=127.0.0.1
DATABASE_URL=postgresql://spectra:spectra@localhost:5432/spectra
DATABASE_DRIVER=pg
