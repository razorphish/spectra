/** Path to merged **public limited** API spec (after `nx run openapi:merge`). */
export const MERGED_PUBLIC_SPEC_RELATIVE =
  'packages/openapi/dist/spectra-public-api.json';

/** Path to merged **full** API spec for `GET /integration/openapi.json` (after merge). */
export const MERGED_INTEGRATION_SPEC_RELATIVE =
  'packages/openapi/dist/spectra-integration-api.json';
